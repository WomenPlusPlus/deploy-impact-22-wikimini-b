// database calls

const { mwn } = require('mwn');

const wiki = new mwn();

export function login(username, password) {
/*    wiki.setOptions(username,password);
    wiki.login();*/
    return "okay"
}
